## alpine-docker-git-make

Container that is extending [alpine-docker/git](https://github.com/alpine-docker/git) with ```make```.

### Build

Building a new docker container.

```
make build
```

### Publish

Publishing (and building) a new docker container.

```
make publish
```
